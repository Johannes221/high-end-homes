# Cloudflare Pages Configuration
# Note: This is for static export only. Current app needs SSR and won't work on Pages.

name = "high-end-homes"
compatibility_date = "2024-01-01"

[site]
bucket = "./out"
